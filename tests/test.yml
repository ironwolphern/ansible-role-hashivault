---
- name: Test playbook
  hosts: vault
  gather_facts: true

  tasks:
  roles:
    - ansible-role-hashivault
  vars:
    hashivault__version: 1.15.1
    hashivault__server_fqdn: vault.example.local
